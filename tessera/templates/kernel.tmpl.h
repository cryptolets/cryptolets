#ifndef ${kernel_upper}_H
#define ${kernel_upper}_H

#include <ac_int.h>
#include "params.h"

template <{TEMPLATE_PARAMS}>
{OUTPUT_TYPE} ${kernel}<{TEMPLATE_PARAMS}>(
    {INPUT_TYPE},
    {INPUT_TYPE},
    {INPUT_TYPE}
) {
    // TODO: Implement kernel
}

#endif // ${kernel_upper}_H
