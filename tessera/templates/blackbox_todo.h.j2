#ifndef _{{ kernel | upper }}_IMPL_H_
#define _{{ kernel | upper }}_IMPL_H_

// TODO: a dry run wrote this placeholder. The real header describes {{ kernel }}
// as it was built, carrying the ports, latency and area Catapult blackboxes it by.
//
// Build it first, which a run of this kernel does on its own:
//   {{ manifest }}

#error "{{ kernel }} is not built, so it cannot be blackboxed"

#endif /* _{{ kernel | upper }}_IMPL_H_ */
