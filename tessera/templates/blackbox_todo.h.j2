#ifndef _{{ kernel | upper }}_IMPL_H_
#define _{{ kernel | upper }}_IMPL_H_

// TODO: a gen only run wrote this placeholder. The real header describes
// {{ kernel }} as it was built, carrying the ports, latency and area Catapult
// blackboxes it by. Run past gen so the designs of {{ kernel }} get built.

#error "{{ kernel }} is not built, so it cannot be blackboxed"

#endif /* _{{ kernel | upper }}_IMPL_H_ */
