#ifndef _{{ kernel | upper }}_H_
#define _{{ kernel | upper }}_H_

#include <ac_int.h>
#include "params.h"

template<{{ template_params }}>
class {{ kernel }}_impl {
public:
    void run(
{% for arg in inputs %}        const {{ arg.decl_type }} {{ arg.name }},
{% endfor %}        {{ decl_output_type }} &z
    ) {
        // TODO: Implement kernel
    }
};

#endif /* _{{ kernel | upper }}_H_ */
