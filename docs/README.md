# [WIP] Tessera Documentation

Setup and Quick Start are in the root README. Plan for detailed documentation. 

- `usage.md` — how to run the full flow, partial runs with `--to`, `--from` and `--only`, reruns, and the analyze tool.
- `flow.md` — the steps a design goes through, done markers, the build directory, packages and blackboxing.
- `sweeps.md` — the sweep file format, width maps, how designs are derived and filtered, and the flags.
- `new.md` — how to make a new kernel: scaffold, `kernel.yaml`, implementation, testbench, samples, README and sweep.
- `conventions.md` — naming, generated code, ports, Montgomery domain handling, code style and the kernel README format.
- `verification.md` — the testbench contract, test samples, the reference library, gate-level simulation and power.
